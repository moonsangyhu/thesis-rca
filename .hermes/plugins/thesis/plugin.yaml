name: thesis
version: 0.1.0
description: "Agent-free /thesis Slack control-plane command. Signs a CommandEnvelope from the gateway-verified request identity and calls the campaign Controller over its bounded Unix socket — never through the model/agent/Codex loop."
author: thesis-rca
kind: standalone
platforms:
  - macos
  - linux
