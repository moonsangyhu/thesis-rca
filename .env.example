# 실험 비밀값 템플릿. 이 파일을 `.env`로 복사하고 값을 채운다(.env는 gitignore됨).
# run_experiment.py가 시작 시 load_dotenv()로 .env를 환경에 주입한다.

# RCA LLM (모델 고정: gpt-4o-mini, provider openai)
OPENAI_API_KEY=

# (선택) System B RAG/Anthropic 경로를 쓸 경우
# ANTHROPIC_API_KEY=
