# Project-scoped Codex configuration. Loaded only for a trusted repository.
project_doc_max_bytes = 65536

[features]
hooks = true
multi_agent = true

[agents]
max_concurrent_threads_per_session = 6
interrupt_message = true
